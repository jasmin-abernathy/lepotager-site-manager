package org.lepotager.sitemanager.ui

/**
 * Classifier alias used by Screens.kt for a composable Column receiver. Compose's
 * `Column(...)` function lives in the callable namespace, so both names coexist.
 */
typealias Column = androidx.compose.foundation.layout.ColumnScope
